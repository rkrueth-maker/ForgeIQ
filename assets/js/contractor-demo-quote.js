(()=>{const money=n=>new Intl.NumberFormat('en-US',{style:'currency',currency:'USD'}).format(n);const base=Number(document.body.dataset.base||0);const checks=[...document.querySelectorAll('[data-upgrade]')];const total=document.querySelector('[data-total]');const selected=document.querySelector('[data-selected-options]');function update(){let sum=base;const names=[];checks.forEach(c=>{if(c.checked){sum+=Number(c.dataset.price||0);names.push(`${c.dataset.label} (${money(Number(c.dataset.price||0))})`)}});if(total)total.textContent=money(sum);if(selected)selected.textContent=names.length?names.join(', '):'No optional upgrades selected.'}checks.forEach(c=>c.addEventListener('change',update));document.querySelectorAll('[data-print]').forEach(b=>b.addEventListener('click',()=>window.print()));document.querySelectorAll('[data-copy]').forEach(b=>b.addEventListener('click',async()=>{const text=document.querySelector('main').innerText;try{await navigator.clipboard.writeText(text);b.textContent='Copied';setTimeout(()=>b.textContent='Copy Quote Text',1600)}catch{b.textContent='Select and copy manually'}}));update();})();