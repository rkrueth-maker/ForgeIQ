name: Finalize Navigation Cache Source

on:
  pull_request:
    branches: [main]

permissions:
  contents: write

concurrency:
  group: finalize-navigation-cache-source-${{ github.event.pull_request.number }}
  cancel-in-progress: false

jobs:
  finalize:
    if: github.event.pull_request.head.repo.full_name == github.repository && github.event.pull_request.head.ref == 'agent/navigation-and-module-cache-speed'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0
      - name: Verify source behavior
        shell: bash
        run: |
          set -euo pipefail
          node scripts/verify-change-governance.js
          node scripts/verify-navigation-cache-performance.js
          node scripts/verify-unified-app-architecture.js
          node scripts/verify-owner-portal-next.js
          node scripts/verify-business-office.js
          node scripts/verify-unified-client-routing-runtime.js
          node scripts/verify-task-messaging.js
          node scripts/verify-task-messaging-hardening.js
          node scripts/verify-unified-app-ux-complete.js
      - name: Remove generated evidence and preserve focused test entry
        shell: bash
        run: |
          set -euo pipefail
          git fetch origin main
          git checkout origin/main -- artifacts/business-office/verification.json artifacts/task-messaging/verification.json package.json
          python3 - <<'PY'
          from pathlib import Path
          path=Path('package.json')
          text=path.read_text(encoding='utf-8')
          old='"test:business-office": "node scripts/verify-one-shot-ux.js'
          new='"test:business-office": "node scripts/verify-navigation-cache-performance.js && node scripts/verify-one-shot-ux.js'
          if text.count(old)!=1:
              raise SystemExit(f'Expected one Business Office test marker, found {text.count(old)}')
          path.write_text(text.replace(old,new,1),encoding='utf-8')
          PY
          rm -f artifacts/change-governance/verification.json
          rm -f artifacts/product-architecture/verification.json
          rm -f artifacts/task-messaging/hardening-verification.json
          rm -f artifacts/unified-app-ux/verification.json
          rm -f artifacts/unified-shell/assembly.json
          rm -f artifacts/unified-source-assembly/verification.json
          rm -f launch-control/evidence/unified-app-architecture-verification.json
          rm -f .github/workflows/finalize-navigation-cache-source.yml
          node scripts/verify-navigation-cache-performance.js
          git diff --check
      - name: Commit clean source branch
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'Keep navigation cache PR source-only'
          git push origin HEAD:${{ github.event.pull_request.head.ref }}
